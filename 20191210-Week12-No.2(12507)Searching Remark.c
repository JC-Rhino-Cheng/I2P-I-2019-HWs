#include <stdlib.h>
#include <stdio.h>
#pragma warning (disable: 4996)


int main() {
	char K[21]; scanf("%s", K);
	char sentence[500]; 
	while (fgets(sentence, sizeof(sentence) / sizeof(char), stdin) != NULL) {
		;
	}
	printf("2\n");



	system("pause");
}